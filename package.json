{
  "name": "facebook-marketplace-vehicle-scanner",
  "version": "22.0.0",
  "private": true,
  "scripts": {
    "check": "node --check background.js && node --check content.js && node --check popup.js && node --check category-detector.js && node --check payload-normalizer.js",
    "test": "node --test",
    "validate:manifest": "node scripts/validate-manifest.js",
    "validate": "npm run check && npm test && npm run validate:manifest"
  }
}
